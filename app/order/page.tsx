import CreateOrder from "@/components/order/CreateOrder";

export const metadata = {
  title: "Generate Create Order",
};

export default async function GenerateOrderPage() {
  return <CreateOrder />;
}
